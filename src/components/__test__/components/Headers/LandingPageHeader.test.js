import { shallow } from 'enzyme'
import LandingPageHeader from '../../../Headers/LandingPageHeader'
import React from 'react';

let wrapper;

beforeEach(() => {
    wrapper = shallow(<LandingPageHeader />)
})

describe("Main Component", () => {
    it('should render', () => {

    });
})