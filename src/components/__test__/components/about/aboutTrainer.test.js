import { shallow } from 'enzyme'
import { AboutTrainer } from '../../../about/aboutTrainer'
import React from 'react';

let wrapper;

beforeEach(() => {
    wrapper = shallow(<AboutTrainer />)
})

describe("Main Component", () => {
    it('should render', () => {

    });
})