import { shallow } from 'enzyme'
import { ListClassOfTrainer } from '../../../AssignmentTrainer/ListClassOfTrainer'
import React from 'react';

let wrapper;

beforeEach(() => {
    wrapper = shallow(<ListClassOfTrainer />)
})

describe("Main Component", () => {
    it('should render', () => {

    });
})