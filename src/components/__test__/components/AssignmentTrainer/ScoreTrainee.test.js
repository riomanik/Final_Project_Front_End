import { shallow } from 'enzyme'
import { ScoreTrainee } from '../../../AssignmentTrainer/ScoreTrainee'
import React from 'react';

let wrapper;

beforeEach(() => {
    wrapper = shallow(<ScoreTrainee />)
})

describe("Main Component", () => {
    it('should render', () => {

    });
})