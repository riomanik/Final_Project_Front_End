import { shallow } from 'enzyme'
import { videoTrainee } from '../../../Lessons/videoTrainee'
import React from 'react';

let wrapper;

beforeEach(() => {
    wrapper = shallow(<videoTrainee />)
})

describe("Main Component", () => {
    it('should render', () => {

    });
})