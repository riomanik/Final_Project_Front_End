import { shallow } from 'enzyme'
import SectionCarousel from '../../../landingPage/SectionCarousel'
import React from 'react';

let wrapper;

beforeEach(() => {
    wrapper = shallow(<SectionCarousel />)
})

describe("Main Component", () => {
    it('should render', () => {

    });
})