import { shallow } from 'enzyme'
import { ChatTrainer } from '../../../chat/ChatTrainer'
import React from 'react';

let wrapper;

beforeEach(() => {
    wrapper = shallow(<ChatTrainer />)
})

describe("Main Component", () => {
    it('should render', () => {

    });
})